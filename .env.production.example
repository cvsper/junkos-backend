# =============================================================================
# Umuve Backend - Production Environment Variables
# =============================================================================
# This file documents EVERY env var needed for a production deployment.
#
# HOW TO USE:
#   1. Copy this file and fill in real values (never commit the real file).
#   2. Set each variable in your hosting provider's dashboard (Render, etc.).
#   3. Variables marked CRITICAL must be set before launch.
# =============================================================================

# === CRITICAL: Must be set before launch ===

# JWT secret for token signing (64-char hex).
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=<generate-64-char-hex>

# Secret required by POST /api/auth/seed-admin (32-char hex).
# Generate with: python -c "import secrets; print(secrets.token_hex(16))"
ADMIN_SEED_SECRET=<generate-32-char-hex>

# Flask secret key for session signing.
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=<generate-64-char-hex>

# API key for legacy endpoints (X-API-Key header).
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=<generate-urlsafe-token>

# Must be "production" in deployed environments
FLASK_ENV=production

# === CORS ===
# Comma-separated allowed origins. NEVER use * in production.
CORS_ORIGINS=https://platform-olive-nu.vercel.app,https://landing-page-premium-five.vercel.app,https://goumuve.com

# === Stripe (switch to live keys before launch) ===
STRIPE_SECRET_KEY=sk_live_...
STRIPE_PUBLISHABLE_KEY=pk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

# === Communications: Twilio SMS ===
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# === Communications: Email (Resend preferred, SendGrid fallback) ===
RESEND_API_KEY=
SENDGRID_API_KEY=
EMAIL_FROM=bookings@goumuve.com
EMAIL_FROM_NAME=Umuve

# === Push Notifications (APNs for iOS) ===
APNS_KEY_ID=
APNS_TEAM_ID=
APNS_KEY_PATH=AuthKey.p8
APNS_BUNDLE_ID=com.goumuve.app

# === Database ===
# PostgreSQL connection URL (set automatically by Render).
# Format: postgresql://user:password@host:5432/umuve
DATABASE_URL=postgresql://...

# === Monitoring ===
# Sentry DSN for error tracking. Leave blank to disable.
SENTRY_DSN=

# === Pricing ===
BASE_PRICE=50.0

# === Server ===
# Port (set automatically by Render)
# PORT=8080
